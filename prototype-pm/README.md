# prototype-pm (legacy redirect)

This folder used to hold the Data-Quality Prototype. It moved to [`../prototype-quality/`](../prototype-quality/) during the May 2026 repo cleanup — `pm` was ambiguous (project management? property management?), so the folder was renamed to describe its actual focus.

The [`index.html`](index.html) here is a tiny redirect stub kept so old bookmarks to `https://bbl-dres.github.io/geo-check/prototype-pm/` keep working — it sends visitors to `…/prototype-quality/`.

**Do not add new files here.** Add them to [`../prototype-quality/`](../prototype-quality/) instead.

This stub can be deleted once the redirect is no longer needed (e.g. once analytics show no traffic on the old path for a few months).
